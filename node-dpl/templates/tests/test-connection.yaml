apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "node-dpl.fullname" . }}-test-connection"
  labels:
    {{- include "node-dpl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "node-dpl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
